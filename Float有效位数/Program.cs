using System;

namespace Float有效位数
{
    class Program
    {
        //mysql 默认的float类型都只能存6个数字（包括小数点前后的位数）
        static void Main(string[] args)
        {

            //Console.WriteLine(a.ToString());
            Console.ReadKey();
        }
    }
}
